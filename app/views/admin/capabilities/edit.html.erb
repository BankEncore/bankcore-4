<% content_for :title, "Edit Capability - BankCORE" %>

<%= render "shared/internal/page_header",
  title: "Edit capability",
  subtitle: @capability.code,
  actions: capture {
    link_to "Capability detail", admin_capability_path(@capability), class: bc_button_class(:secondary)
  } %>

<section class="mt-8">
  <%= render "form" %>
</section>
