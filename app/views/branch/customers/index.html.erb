<% content_for :title, "Customer Servicing - BankCORE" %>

<%= render "shared/internal/page_section",
  title: "Customer servicing",
  content: capture {
    form_with url: branch_customers_path, method: :get, class: "grid gap-4 md:grid-cols-[1fr_auto]" do |form|
      concat(content_tag(:div) do
        safe_join([
          form.label(:query, "Customer search", class: "block text-sm font-medium text-slate-700"),
          form.text_field(:query, value: @query, placeholder: "Party #123 or Jane Doe", class: "mt-1 w-full rounded border border-slate-300 px-3 py-2"),
          content_tag(:p, class: "mt-1 text-xs text-slate-500") do
            safe_join([
              "Examples: ",
              content_tag(:code, "Party #123"),
              ", ",
              content_tag(:code, "123"),
              ", ",
              content_tag(:code, "Jane Doe"),
              ", or a legal entity name."
            ])
          end
        ])
      end)
      concat(content_tag(:div, class: "self-end") { form.submit("Search", class: bc_button_class(:primary)) })
    end
  } %>

<section class="mt-6 overflow-hidden <%= bc_panel_class(tone: :default) %>">
  <% if @search.nil? %>
    <div class="p-6 text-sm text-slate-600">Enter a party id or name to begin customer servicing.</div>
  <% elsif @search.rows.any? %>
    <table class="min-w-full divide-y divide-slate-200 text-sm">
      <thead class="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500">
        <tr>
          <th class="px-5 py-3">Customer</th>
          <th class="px-5 py-3">Type</th>
          <th class="px-5 py-3">Profile</th>
          <th class="px-5 py-3">Actions</th>
        </tr>
      </thead>
      <tbody class="divide-y divide-slate-100">
        <% @search.rows.each do |party| %>
          <tr>
            <td class="px-5 py-4">
              <div class="font-medium text-slate-950"><%= party.name %></div>
              <div class="text-xs text-slate-500">Party #<%= party.id %></div>
            </td>
            <td class="px-5 py-4"><%= party.party_type %></td>
            <td class="px-5 py-4">
              <% if party.individual_profile %>
                <%= [party.individual_profile.first_name, party.individual_profile.last_name].compact.join(" ") %>
              <% else %>
                Not recorded
              <% end %>
            </td>
            <td class="px-5 py-4">
              <%= link_to "Open 360", branch_customer_path(party), class: bc_button_class(:primary) %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% else %>
    <div class="p-6 text-sm text-slate-600">No customers found for “<%= @search.query %>”.</div>
  <% end %>
</section>
